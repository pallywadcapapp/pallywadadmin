@{
    ViewBag.Title = "Loan Preview";
}

<link href="~/css/index_style.css" rel="stylesheet">
<link href="~/assets/css/imgPreview.css" rel="stylesheet" type="text/css">
<style>
    .hide {
        display: none;
    }

    .select2-selection {
        height: 34px !important;
        font-size: 13px;
        font-family: 'Open Sans', sans-serif;
        border-radius: 0 !important;
        border: solid 1px #c4c4c4 !important;
        padding-left: 4px;
    }

    .select2-selection--multiple {
        height: 154px !important;
        width: 366px !important;
    }

    .select2-selection__choice {
        height: 40px;
        line-height: 40px;
        padding-right: 16px !important;
        padding-left: 16px !important;
        background-color: #CAF1FF !important;
        color: #333 !important;
        border: none !important;
        border-radius: 3px !important;
    }

    .select2-selection__choice__remove {
        float: right;
        margin-right: 0;
        margin-left: 2px;
    }

    .select2-search--inline .select2-search__field {
        line-height: 40px;
        color: #333;
        width: 100% !important;
    }

    .select2-container:hover,
    .select2-container:focus,
    .select2-container:active,
    .select2-selection:hover,
    .select2-selection:focus,
    .select2-selection:active {
        outline-color: transparent;
        outline-style: none;
    }

    .select2-results__options li {
        display: block;
    }

    .select2-selection__rendered {
        transform: translateY(2px);
    }

    .select2-selection__arrow {
        display: none;
    }

    .select2-results__option--highlighted {
        background-color: #CAF1FF !important;
        color: #333 !important;
    }

    .select2-dropdown {
        border-radius: 0 !important;
        box-shadow: 0px 3px 6px 0 rgba(0, 0, 0, 0.15) !important;
        border: none !important;
        margin-top: 4px !important;
        width: 366px !important;
    }

    .select2-results__option {
        font-family: 'Open Sans', sans-serif;
        font-size: 13px;
        line-height: 24px !important;
        vertical-align: middle !important;
        padding-left: 8px !important;
    }

    .select2-results__option[aria-selected="true"] {
        background-color: #eee !important;
    }

    .select2-search__field {
        font-family: 'Open Sans', sans-serif;
        color: #333;
        font-size: 13px;
        padding-left: 8px !important;
        border-color: #c4c4c4 !important;
    }

    .select2-selection__placeholder {
        color: #c4c4c4 !important;
    }
    .pull-right{
        margin-right: 10px;
    }
    p{
        margin-left: 5%;
    }

    .gg{
        margin-left: 5%;
    }
</style>
<div class="container-fluid">
    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="~/">Dashboard</a>
        </li>
        <li class="breadcrumb-item active">Loan Preview</li>
    </ol>
    <!-- Example DataTables Card-->
    <div class="card mb-3" id="detail">
        <div class="card-header">
            <i class="fa fa-table"></i><span style="margin-left: 10%;" name="fullname"></span>
            <span> LOAN PREVIEW</span>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-sm-6" id="loanProcessView">
                    <p>
                        <span>Loan Category: </span><span name="category" id="category"></span>
                    </p>
                    <p>
                        <span>Loan ID: </span><span name="loanId" id="loanId"></span>
                    </p>
                    <p>
                        <span>Member : </span><b><span name="fullname" id="fullname"></span></b>
                    </p>
                    <p>
                        <span>Amount: </span><b><span name="amount" id="amount"></span></b>
                    </p>
                    <p>
                        <span>Interest Rate(%): </span><span name="loaninterest" id="loaninterest"></span>
                    </p>
                    <p>
                        <span>Request Interest Rate(%): </span><span name="preferredRate" id="preferredRate"></span>
                    </p>
                    <p>
                        <span>Loan Purpose: </span><b><span name="purpose" id="purpose"></span></b>
                    </p>
                    <div class="business">

                        <p>
                            <span> Business Name: </span><b><span name="businessname" id="businessname"></span></b>
                        </p>
                        <p>
                            <span> Business Sector: </span><b><span name="sector" id="sector"></span></b>
                        </p>
                        <p>
                            <span> Years in Business: </span><b><span name="age" id="age"></span></b>
                        </p>
                    </div>
                    <p>
                        <span>Processing Fee: </span><span name="processingFee" id="processingFee"></span>
                    </p>
                    <p>
                        <span>Tenor(Months): </span><span name="duration" id="duration"></span>
                    </p>
                    <p>
                        <span>Requested Date: </span><span name="requestDate" id="requestDate"></span>
                    </p>
                    <p>
                        <span>Approval Date: </span><span name="approvalDate" id="approvalDate"></span>
                    </p>
                    </div>
                    <div class="col-sm-6">
                        <h5>Documents Preview</h5>
                        <div id="docPrev"></div>
                    <h5>Collateral Preview</h5>
                    <div id="collPrev"></div>
                    </div>
            </div>
        </div>
</div>


<div>
               <div class="col-sm-12">
            <button type="button" class="btn btn-danger pull-right" data-toggle="modal" data-target="#loadReviewModal" onclick="__saveReview('Decline')" id="decline">Decline</button>
            <button type="button" class="btn btn-primary approve" onclick="saveReview('Approve')" id="processed">Process</button>
                    <button type="button" class="btn btn-primary pull-left" onclick="printPage()"><i class="fa fa-print"></i>Print</button>
                </div>
</div>
<div class="card-footer small text-muted">Updated Today at <span id="todayT"></span></div>
</div>

<div id="loadReviewModal" class="modal fade" role="dialog">
    <div class="modal-dialog  modal-lg">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header bg-success">
                <h4 class="modal-title"> <span name="category"></span>Decline Request</h4>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-sm-12">
                        <div>
                            <span>Kindly Provide reasons for declining this loan request</span>
                        </div>
                        <div>
                            <span>Reason : </span><textarea name="reason" id="reason" class="form-control"></textarea>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer bg-success">
                <button type="button" class="btn btn-primary" onclick="declineRequest('Approve')">
                    Decline
                </button>
                <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>

<div id="myModal" class="modal">

    <!-- The Close Button -->
    <span class="close">&times;</span>

    <!-- Modal Content (The Image) -->
    <img class="modal-content" id="img01">

    <!-- Modal Caption (Image Text) -->
    <div id="caption"></div>
</div>

@section Scripts{

<script src="~/Scripts/config.js" type="text/javascript"></script>
<script>
    var loanDetails = '';
    var loanId = qs['loanId'];
        var memberId = qs['memberId'];
        var formr = qs['form'];
    loadView(loanId);

    function printPage(){
        var printwin = window.open("");
printwin.document.write(document.getElementById("detail").innerHTML);
printwin.print();
    }

        function declineRequest(){
            $('#loadReviewModal').pleaseWait();
            var reason = $('#reason').val();
            loanDetails.processState = "Processed";
            loanDetails.processState = "Decline";
            loanDetails.reason = reason;
            loanDetails.approvedBy = localStorage.getItem('uemail');

            url = appConfig.adminURL + 'api/loanrequest/loanrequestdecline?id=' + loanDetails.id +
                '&reason=' + encodeURI(reason);

            $.ajax({
                type: 'PUT',
                url: url,
                data: JSON.stringify(loanDetails),
                contentType: "application/json",
            }).done(function (data) {
                $('#loadReviewModal').pleaseWait('stop');
                toastr.success('Loan Request  processed successfully');
                window.location.href = 'requests';
                //refreshTables();
            }).fail(function (request, message, error) {
                $('#loadReviewModal').pleaseWait('stop');
                toastr.error('error ' + error + 'for processing Loan Request ');
            });
        }

        function saveReview(type) {
            
            //var url = '@Html.Raw(Url.Action("LoanApproval", "loans"))';
            if (formr == 'request') {
                var url = '@Html.Raw(Url.Action("LoanApproval", "loans"))';
                window.location.href = url + '?loanId=' + encodeURIComponent(loanDetails.loanId) + '&memberId=' + encodeURIComponent(loanDetails.memberId);
            }
            

        }
function etype(elementId){    
    var type = $('[id="' + elementId + '"').attr('type');
    return type;
   }

    function loadView(id) {
        $('#loadViewModal').pleaseWait();
        var url = appConfig.adminURL + 'api/loanrequest/loandetail?loanId=' + encodeURIComponent(id);
        $.get(url, function (data) {
            loanDetails = data;
            $('#loadViewModal').pleaseWait('stop');
            try {
                for (var key in data) {
                    if (key == 'insuredname') {
                        var lname = data[key].split(' ')[0];
                        var fname = data[key].split(' ')[1];
                        $('[name="firstname"]').html(fname);
                        $('[name="lastname"]').html(lname);
                    }
                    $('[name="' + key + '"]').html(data[key]);
                    if (key == 'requestDate') {
                        $('[name="' + key + '"]').html(new Date(data[key]).toLocaleDateString());
                    }
                    if (key == 'approvalDate') {
                        if (data[key] == null) {
                            var d = new Date().toISOString().split('T')[0];
                            //$('[name="' + key + '"]').html(new Date(data[key]).toLocaleDateString());
                            $('[id="' + key + '"]').val(d);
                        } else {
                            var d = new Date(data[key]).toISOString().split('T')[0];
                            $('[name="' + key + '"]').html(new Date(data[key]).toLocaleDateString());
                            $('[id="' + key + '"]').val(d);
                        }

                    }
                    if (key == 'category') { 
                        if (data[key] == "Personal Loan") {
                            $('.business').hide()
                        } else { 
                            $('.business').show()
                        }
                    }
                    if (key == 'memberId') {
                        var member = getMemberById(data[key]);
                        var fullname = member.lastname + ' ' + member.firstname + ' ' + member.othernames;
                        //var fullname = getMemberById(data[key]).fullname
                        $('[name="' + key + '"]').html(data[key]);
                        $('[name="fullname"]').html(fullname.toUpperCase());
                    }
                    if (key == 'amount') {
                        $('[name="' + key + '"]').html(currformatter.format(data[key]));
                    }
                    if (key == 'processingFee') {
                        $('[name="' + key + '"]').html(currformatter.format(data[key]));
                    }
                        if (key == 'processState') {
                            if(data[key] == 'Processed'){
 $('#decline').hide();
                        $('#processed').hide();
                            }
                       
                        } 
                    if (key == 'loanBal') {
                        $('[name="' + key + '"]').html(currformatter.format(data[key]));
                    }
                    if (key == 'othercoorp') {
                        $('[name="' + key + '"]').html(currformatter.format(data[key]));
                    }
                    if (key == 'loanUserDocuments') {
                        var docList = data[key];
                        loadUserDocuments(data[key]);
                    }
                    if (key == 'loanUserCollaterals') {
                        //loanUserCollaterals(data[key]);
                            
                            
                    }
                        if (key == 'collateralId') {
                            var docList = data[key];
                            singleLoanUserCollaterals(data[key]);
                        }
                }
            } catch (e) {
                console.log(e);
            }
        }).fail(function (xhr, status, error) {
            $('#loadViewModal').pleaseWait('stop');
        });
    }

        function loanUserCollaterals(coll) {
                var collPrevElements = '<div class="row">';
                var $collElement = document.getElementById('collPrev');
            var size = coll.length;
            if (size < 1) {
                collPrevElements = '<h6>No User Collateral Uploaded</h6>'
                    $collElement.innerHTML = collPrevElements + '</div>';
            } else {
                $.each(coll, function (e) {
                    var ecoll = coll[e];
                    $.get(appConfig.adminURL + 'api/collaterals/userCollById?id=' + ecoll.userCollaterallId, function (data) {
                        collPrevElements += '<div class="col-sm-6 card"><img style="width:150px;height:150px;" src="' + appConfig.userURL +
                            'api/documents/fileuploads?filepath=' + data.url + '" /></div>';

                        if (e >= docs.length) {
                            $collElement.innerHTML = collPrevElements + '</div>';
                        } else {
                            $collElement.innerHTML = collPrevElements + '</div>';
                        }

                    })

                });
                }
            }

        function singleLoanUserCollaterals(coll) {
            var collPrevElements = '<div class="row">';
            var $collElement = document.getElementById('collPrev');
            var size = coll.length;
            if (coll == null) {
                collPrevElements = '<h6>No User Collateral Uploaded</h6>'
                $collElement.innerHTML = collPrevElements + '</div>';
            } else {
                //$.each(coll, function (e) {
                    var ecoll = coll;
                var url = '@Html.Raw(Url.Action("images", "assets"))';
                $.get(appConfig.adminURL + 'api/collateral/Files?id=' + ecoll, function (data) {
                    $.each(data, function (e) {
                        console.log(data[e])
                        if (data[e].fileurl.indexOf('.pdf') == -1) {
                            collPrevElements += '<div class="col-sm-6 card"><img onclick="previewImage(this)" class="myImg" style="width:150px;height:150px;" src="' + appConfig.userURL +
                                'api/documents/fileuploads?filepath=' + data[e].fileurl + '" /></div>';
                        } else { 
                            collPrevElements += '<div class="col-sm-6 card"><img onclick="previewImage(this)" class="myImg" style="width:150px;height:150px;" src="' +
                                url + '/pdf.png" '+ 'alt=' + appConfig.userURL +
                                'api/documents/fileuploads?filepath=' + data[e].fileurl + '" /></div>';
                        }
                       
                        $collElement.innerHTML = collPrevElements + '</div>';
                    });
                    })

               // });
            }
        }
            function loadUserDocuments(docs) {
                var docPrevElements = '<div class="row">';
                var $docElement = document.getElementById('docPrev');
                var size = docs.length;
                if (size < 1) {
                    collPrevElements = '<h6>No User Document Uploaded</h6>'
                    $collElement.innerHTML = collPrevElements + '</div>';
                } else {
                    $.each(docs, function (e) {
                        var edocs = docs[e];
                        $.get(appConfig.adminURL + 'api/documents/userDocById?id=' + edocs.userDocumentlId, function (data) {
                            if (data.url.indexOf('.pdf') == -1) {
                                docPrevElements += '<div class="col-sm-6 card"><img onclick="previewImage(this)" class="myImg" style="width:150px;height:150px;" src="'
                                    + appConfig.userURL + 'api/documents/fileuploads?filepath=' + data.url + '" /></div>';
                            } else {
                            docPrevElements += '<div class="col-sm-6 card"><img onclick="previewImage(this)" class="myImg" style="width:150px;height:150px;" src="'
                                + 'assets/images/pdf.png' + '" /></div>';
                            }
                        
                            //$docElement.innerHTML = docPrevElements;

                            if (e >= docs.length) {
                                $docElement.innerHTML = docPrevElements + '</div>';
                            } else {
                                $docElement.innerHTML = docPrevElements + '</div>';
                            }

                        })

                    });
                }
            }


        function showError(error, value, xhr) {
            $('#strWorkspace').pleaseWait('stop');
            toastr.error('unable to process request approval ');
            toastr.error(error.responseText);
            console.log(error);
        }
        var modal = document.getElementById("myModal");
    function previewImages(id) {
            var modal = document.getElementById("myModal");

            // Get the image and insert it inside the modal - use its "alt" text as a caption
            var img = document.getElementById("myImg");
            var modalImg = document.getElementById("img01");
            var captionText = document.getElementById("caption");
            img.onclick = function () {
                modal.style.display = "block";
                modalImg.src = this.src;
                captionText.innerHTML = this.alt;
            }

            // Get the <span> element that closes the modal
            var span = document.getElementsByClassName("close")[0];

            // When the user clicks on <span> (x), close the modal
            span.onclick = function () {
                modal.style.display = "none";
            }
    }

    function previewImage(id) {
            
            var img = document.getElementById("myImg");
            var modalImg = document.getElementById("img01");
            var captionText = document.getElementById("caption");
            if (id.src.indexOf('pdf.png') == -1) {
                modal.style.display = "block";
                modalImg.src = id.src;
                captionText.innerHTML = id.alt;
        } else {
            window.location.href= id.alt
        }
          
    }

        var span = document.getElementsByClassName("close")[0];

        // When the user clicks on <span> (x), close the modal
        span.onclick = function () {
            modal.style.display = "none";
        }
</script>


}